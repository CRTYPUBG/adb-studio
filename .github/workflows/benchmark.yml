name: Benchmark
on: { pull_request: {}, workflow_dispatch: {} }
jobs:
  windows:
    uses: ./.github/workflows/_windows.yml
    with: { configuration: Release, artifact-name: benchmark-build, command: benchmark }
