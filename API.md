# API

The current public presentation API is the versioned `ADBStudio.Presentation` QML module exposing
uncreatable `ThemeService` and `DeviceDashboardViewModel` types supplied by the application.
Device, mirror, settings, diagnostics and plugin contracts will be published with their milestone.
API/ABI compatibility policy follows semantic versioning and requires contract tests.
